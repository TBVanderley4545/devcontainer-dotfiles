- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.config/nvim:
      path: nvim
      create: true
    ~/.gitconfig:
    ~/.ripgreprc:
    ~/.tmux.conf: tmux
    ~/.zsh:
    ~/.zshrc:

- shell:
    - command: ./nvm-installer
      stdout: true
